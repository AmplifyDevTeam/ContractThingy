rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All access goes through the ContractOS API (Firebase Admin SDK).
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

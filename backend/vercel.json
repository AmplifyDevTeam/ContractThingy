{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "hono",
  "installCommand": "if [ -f ../package-lock.json ]; then cd .. && npm install --workspace=backend; else npm install; fi",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "vercel-out"
}
